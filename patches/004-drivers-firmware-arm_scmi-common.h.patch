--- /tmp/linux-source-6.18-pristine/linux-source-6.18/drivers/firmware/arm_scmi/common.h	2026-01-11 08:26:20.000000000 -0600
+++ drivers/firmware/arm_scmi/common.h	2026-02-08 16:18:39.208000000 -0600
@@ -493,6 +493,9 @@
 		return -ENOMEM;						       \
 									       \
 	device_set_of_node_from_dev(&spdev->dev, dev);			       \
+	/* Propagate fwnode for ACPI devices (no of_node) */		       \
+	if (!spdev->dev.of_node && dev->fwnode)				       \
+		spdev->dev.fwnode = dev->fwnode;				       \
 									       \
 	strans.supplier = dev;						       \
 	memcpy(&strans.desc, &(__desc), sizeof(strans.desc));		       \
